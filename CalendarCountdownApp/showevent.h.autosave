#ifndef SHOWEVENT_H
#define SHOWEVENT_H

#endif // SHOWEVENT_H
#ifndef EVENTWINDOW_H
#define EVENTWINDOW_H

#include <QWidget>
#include <QListView>
#include <QStringListModel>

namespace Ui {
class DisplayEvent;
}

class DisplayEvent : public QWidget {
    Q_OBJECT

public:
    explicit DisplayEvent(QWidget *parent = nullptr);
    ~DisplayEvent();

private:
    Ui::DisplayEvent *ui;
};

#endif // EVENTWINDOW_H
